<div class="container mt-5">
  <h1 class="text-center mb-4">🎬 Liste de films</h1>

  <div class="text-center mb-4">
    <%= link_to "Nouvelle Liste", new_list_path, class: "btn btn-primary btn-lg" %>
  </div>

  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    <% @lists.each do |list| %>
      <div class="col">
        <div class="card shadow-sm h-100">
          <div class="card-body text-center">
            <h5 class="card-title">
              <%= link_to list.name, list_path(list), class: "stretched-link text-decoration-none text-light" %>
            </h5>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
