<div class="container mt-5">
  <h1 class="text-center mb-4">🎥 Ajouter un signet à <%= @list.name %></h1>

  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-body">
          <%= simple_form_for [@list, @bookmark] do |f| %>
            <div class="mb-3">
              <%= f.association :movie, collection: Movie.all, prompt: "Choisis un film", input_html: { class: "form-select" } %>
            </div>
            <div class="mb-3">
              <%= f.input :comment, label: "Commentaire", input_html: { class: "form-control" } %>
            </div>
            <div class="d-grid mb-3">
              <%= f.button :submit, "Ajouter un signet", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>
          <div class="d-grid">
            <%= link_to "Retour", list_path(@list), class: "btn btn-secondary btn-lg" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>


